version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: dockerfile
    environment:
      - NODE_ENV=development
    ports:
      - "3001:3001"
    volumes:
      - .:/app
      - /node_modules
      - /var/www/html/SiteSculptor:/some/container/path
    command: npm run dev
