version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: dockerfile
    environment:
      - NODE_ENV=development
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - /node_modules
    command: npm run dev
